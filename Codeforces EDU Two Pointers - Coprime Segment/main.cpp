#include <bits/stdc++.h>
#define int long long
using namespace std;

int arr[100005];

signed main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    int n;
    cin >> n;
    int i;
    for (i=1;i<=n;i++)
        cin >> arr[i];

    int L, R=1;
    for (L=1;L<=n;L++) {
        int nowgcd;

    }
    return 0;
}
